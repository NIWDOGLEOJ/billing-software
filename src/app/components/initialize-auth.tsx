import { useEffect } from 'react';
import { User, Permission } from '../contexts/auth-context';
import { hashPassword, isHashed } from '../utils/crypto';

export function InitializeAuth() {
  return null;
}